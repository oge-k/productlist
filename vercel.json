
{
  "builds": [
    {
      "src": "./package.json",
      "use": "@vercel/node"
    }
  ],
  "routes": [
   {
       "src": "/(.*)",
       "dest": "/index.html"
   }
],
  "installCommand": "npm install",
  "buildCommand": "npm run build",
   "outputDirectory": "dist"
}
